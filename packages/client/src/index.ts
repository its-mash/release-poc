import { coreDummy } from "@its-mash/core";

// Dummy code for client
export function clientDummy(): string {
  return "client dummy +" + coreDummy();
}

// Dummy code for client
export function clientDummyV2(): string {
  return "client dummy +" + coreDummy();
}
